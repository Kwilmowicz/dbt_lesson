version: 2
models:
  - name: int_orders_margin
    columns:
      - name: orders_id
        tests:
        - unique
        - not_null
  - name: int_orders_operational
    columns:
      - name: orders_id
        tests:
        - unique
        - not_null
  - name: int_campaigns
    columns:
      - name: date_date
        tests:
        - unique
        - not_null
      - name: campaign_key
        tests:
        - unique
        - not_null
        

      